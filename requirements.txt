anthropic>=0.40.0
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Optional (headless fetch fallback for JS-rendered / bot-blocked sites).
# Install with: pip install playwright && playwright install chromium
# playwright>=1.45.0
